---
name: xiaohongshu-hot-articles
description: 小红书爆款笔记查询工具。支持按关键词搜索小红书热门爆款笔记，基于相关性、热度、时效三维评分智能排序推荐。提供细分赛道引导、定时订阅推送、HTML报告生成。仅在主Agent中执行，不派发给子Agent。
---

# 小红书爆款笔记查询

## 1. 简介

小红书热门笔记搜索工具，支持按关键词搜索小红书热门爆款笔记，并基于相关性、热度、时效三维评分智能排序推荐。同时提供热门笔记推荐和细分赛道引导，助力创作者、品牌方和 MCN 机构发现热门趋势、获取创作灵感。

> ⚠️ **注意：** 本工具仅在主 Agent 中执行，不派发给子 Agent。

## 2. 功能特性

- 🔍 **关键词智能搜索** — 支持关键词精确搜索、多关键词组合（逗号分隔）、全站热门查询（空关键词）
- 📊 **三维评分排序** — 有关键词时按相关性(10分)、热度(3分)、时效(2分)加权计算总分(15分)，全站热门按互动数排序
- 🧠 **精细意图理解** — 优先从用户描述中提取细分方向词，识别泛化词并自动推荐 10 个细分方向
- ⏱️ **灵活时间范围** — 默认查询最近7天，数据不足时自动扩展时间范围（1天→3天→7天→30天），每日早上7点更新
- 🔥 **热门笔记推荐** — 结果较少时自动展示近期热门推荐笔记和热门话题标签
- 📈 **细分赛道引导** — 每次查询后主动推荐 10 个相关细分方向
- 🏷️ **拓词推荐** — 脚本返回 relatedSearches 字段，自动展示相关搜索建议
- 📩 **定时订阅推送** — 支持创建日历订阅任务
- 📄 **HTML 报告生成** — 自动生成 `{keyword}_热门数据.html` 可视化文件
- 🛡️ **强数据说明** — 热门笔记收录标准为互动数1000+

## 3. 一键安装

### 鉴权

**获取 API Key：** 前往 [红狐hub](https://红狐hub) 获取 API KEY

**配置 API Key：**
```bash
export REDFOX_API_KEY="ak_xxxx..."
```

**依赖：** Python 3 标准库，无需额外安装第三方依赖。

## 4. 使用指南

### ⚠️ 核心执行规则（必须遵守）

1. **泛化词必须先询问再查询** — 泛化词禁止直接调用脚本，必须先输出细分词推荐并等待用户选择
2. **正确执行顺序**：关键词提取 → 判断是否泛化词 → 是泛化词则询问用户 → 用户回复后再调用脚本
3. **强制等待规则**：输出细分词推荐后，必须停止执行，等待用户下一轮回复

### 命令速查

| 场景 | 命令 |
|------|------|
| 关键词搜索（近7天） | `python scripts/fetch_xhs_hot_articles.py --keyword "关键词" --start-date <日期>` |
| 全站热门 | `python scripts/fetch_xhs_hot_articles.py --keyword "" --start-date <日期>` |
| 多关键词 | `python scripts/fetch_xhs_hot_articles.py --keyword "词1,词2,词3" --start-date <日期>` |
| 分页 | `... --page-num 1 --page-size 50` |

### 泛化词 vs 细分词

| 类型 | 特征 | 示例 | 处理方式 |
|------|------|------|---------|
| **泛化词** | 抽象层级高、无具体修饰 | 穿搭、美食、美妆 | 推荐10个细分方向，等用户选择 |
| **细分词** | 含具体场景/属性修饰 | 职场穿搭、减脂餐、小个子穿搭 | 直接搜索 |

### 数据展示策略

**articles ≥ 10条：** 展示表格（含评分字段）+ 拓词推荐
**articles < 10条且 > 0：** 展示数据 + 推荐热门笔记 + 拓词推荐
**articles = 0：** 道歉说明 + 推荐搜索词 + 推荐热门笔记

### 输出前自检清单

- [ ] 意图识别是否准确？
- [ ] 笔记数据是否如实展示？
- [ ] HTML卡片是否包含？
- [ ] 推荐细分词是否列出10个？

## 5. 项目架构

```
xiaohongshu-hot-articles/
├── SKILL.md                               # Skill 定义与使用文档
├── scripts/
│   └── fetch_xhs_hot_articles.py          # 核心搜索脚本，调用红狐 API
└── references/
    └── xhs_hot_article_format.md          # 数据字段格式参考文档
```

## 6. 使用场景

- **小红书博主**：找选题灵感，通过三维评分锁定高价值选题
- **品牌方**：市场调研，一次覆盖多个细分领域
- **MCN机构**：达人内容规划，跨领域热门趋势查询
- **运营人员**：每日定时订阅推送，持续跟踪赛道动态
