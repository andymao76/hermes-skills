#!/bin/bash
# File-mutation verifier 红色警告显示脚本
# 用法: bash scripts/warning-display.sh "文件路径" "工具名" "原因"
# 或在会话中直接 source 此文件后调用 show_warning 函数

show_warning() {
  local file_path="${1:-未知文件}"
  local tool_name="${2:-patch/write_file}"
  local reason="${3:-安全策略拦截}"

  echo -e '\033[1;31m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'
  echo '⚠  File-mutation verifier 安全拦截'
  echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'
  echo ''
  echo "Hermes 安全策略已拦截以下文件修改："
  echo "  • ${file_path} → [${tool_name}] 被拒绝"
  echo "    原因：${reason}"
  echo ''
  echo '处理规则：'
  echo '  1. Hermes 工具(patch/write_file)的修改未生效'
  echo '  2. 必须以终端命令(cat/grep/sed)实际读取文件内容为准'
  echo '  3. .env 修改请用 sed/重定向，config.yaml 用 hermes config set'
  echo ''
  echo '⚠  不要相信工具返回的"已修改"状态，以终端输出为唯一真相来源！'
  echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m'
}

# 如果直接运行(带参数)则立即显示
if [ $# -gt 0 ]; then
  show_warning "$1" "${2:-patch/write_file}" "${3:-安全策略拦截}"
fi
